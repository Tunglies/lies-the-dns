[package]
name = "lies-the-dns"
version = "0.1.0"
edition = "2024"

[dependencies]
bytes = "1.11.1"
socket2 = { version = "0.6.3", features = ["all"] }
tokio = { version = "1.52.3", features = ["full"] }
